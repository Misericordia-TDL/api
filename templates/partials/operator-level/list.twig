{% extends "base/base.twig" %}

{% block title %}
    Manage Operators
{% endblock %}
{% block container %}
    <div class="jumbotron">
        <h1>List Operator Levels section</h1>
        <p>In this part you can edit, delete or add operators levels</p>
    </div>

    <div class="panel panel-default">
        <div class="panel-heading">Panel heading</div>
        <div class="panel-body"><p>Some default panel content here. Nulla vitae elit libero, a pharetra augue. Aenean
                lacinia bibendum nulla sed consectetur. Aenean eu leo quam. Pellentesque ornare sem lacinia quam
                venenatis vestibulum. Nullam id dolor id nibh ultricies vehicula ut id elit.</p></div>
        <div class="panel-body">
            <a class="btn btn-primary" href="{{ path_for('enter-operator-level-data') }}" role="button">Add a new
                operator level</a>
        </div>
        <table class="table">
            <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>

            {% for operatorLevel in operatorsLevel %}
                <form action="{{ path_for('delete-operator-level') }}" method="post" id="delete-user-{{ operatorLevel._id  }}"
                      name="delete-operatorLevel-{{ operatorLevel._id }}">
                    <tr>
                        <th scope="row">{{ loop.index }}</th>
                        <td>{{ operatorLevel.name }}</td>
                        <td>{{ operatorLevel.description }}</td>
                        <td>
                            <div class="btn-toolbar" role="group" aria-label="...">
                                <a class="btn btn-default" href="{{ path_for('edit-operator-level',{'id': operatorLevel._id }) }}"
                                   role="button">Edit</a>
                                <input type="submit" class="btn btn-default btn-danger" value="Delete">
                            </div>
                        </td>
                    </tr>
                    {{ csrf.field  | raw }}
                </form>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
